export interface Category {
  id: string;
  name: string;
}

export interface NumOfQuestions {
  id: string;
  name: string;
}

export interface TimeLimit {
  id: string;
  name: string;
  label: string;
  stars: string;
}
